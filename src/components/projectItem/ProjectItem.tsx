import React from 'react';
import s from './ProjectItem.module.scss';

const ProjectItem: React.FC<{}> = React.memo(() => {
    return (
        <div className={s.projectItem}>

    </div>)
})

export default ProjectItem;